node_modules
# Keep environment variables out of version control
.env

/generated/prisma

*.pem

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info

# CI test output (generated locally)
server/reports/
client/reports/

# Vite production build (deployed via CI, not committed)
client/dist/

